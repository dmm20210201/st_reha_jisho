<% if rehabilitation.favorited_by?(current_user) %>
  <h5><strong>いいね</strong>
    <%= link_to rehabilitation_favorites_path(rehabilitation.id), method: :delete, remote: true do %>
      ♥<%= rehabilitation.favorites.count %>
    <% end %>
  </h5>
  <% else %>
  <h5><strong>いいね</strong>
    <%= link_to rehabilitation_favorites_path(rehabilitation.id), method: :post, remote: true do %>
      ♡<%= rehabilitation.favorites.count %>
    <% end %>
  </h5>
<% end %>
