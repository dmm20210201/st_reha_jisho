<% @rehabilitations.each do |rehabilitation| %>
<div>
  <!--<p>投稿ユーザー画像：<#%= attachment_image_tag user.profile_image, :profile_image fallback: "no_image.jpg") %></p>-->
  <p>ユーザーネーム：<%= rehabilitation.user.name %></p>
  <p>訓練名：
    <%= link_to rehabilitation_path(rehabilitation.id) do %>
      <%= rehabilitation.training_name %>
    <% end %>
  </p>
  <p>投稿日：<%= rehabilitation.created_at.strftime('%Y/%m/%d') %></p>
</div>
<% end %>